# ブロック崩し - Breakout Game

クラシックなブロック崩しゲームの現代的な実装。パワーアップシステム、複数の難易度、レベルシステムを備えています。

## 主な機能

### 🎮 ゲームプレイ
- **クラシックなブロック崩しゲーム**
  - パドルでボールを跳ね返す
  - すべてのブロックを破壊してクリア
  - ライフ制（3ライフ）

- **3つの難易度**
  - 簡単：遅いボール速度
  - 普通：バランスの良い速度
  - 難しい：高速ボール

- **レベルシステム**
  - クリアするごとにレベルアップ
  - レベルが上がるとブロック数増加
  - 無限に続けられる

### 🎯 操作方法
- **マウス移動** - パドルを左右に動かす
- **クリック** - ボール発射（待機中）
- **SPACE** - 一時停止/再開

### 🧱 ブロックの種類

| 色 | 得点 | 耐久値 | パワーアップ確率 |
|---|---|---|---|
| 🟥 赤 | 10点 | 1回 | 10% |
| 🟧 オレンジ | 20点 | 1回 | 15% |
| 🟨 黄 | 30点 | 1回 | 15% |
| 🟩 緑 | 40点 | 1回 | 20% |
| 🟦 青 | 50点 | 1回 | 20% |
| 🟪 紫 | 60点 | 1回 | 25% |
| ⬜ 銀 | 100点 | 2回 | 30% |
| 🟫 金 | 200点 | 3回 | 40% |

### ⚡ パワーアップシステム

- **🔴 拡大パドル** - パドルが1.5倍に拡大（10秒間）
- **🔵 スローボール** - ボールの速度が半分に（10秒間）
- **🟡 マルチボール** - ボールが3つに分裂
- **🟢 貫通ボール** - ブロックを貫通（10秒間）

### 📊 統計機能
- ハイスコア記録
- 最高レベル
- 総ゲーム数
- クリア数
- LocalStorageで永続化

### 🏆 達成システム
- スコアマイルストーン（1000, 5000, 10000点）
- ブロック破壊数（50, 100個）
- レベルクリア通知
- パワーアップ取得通知
- 新記録達成通知

## ゲームルール

### 基本ルール
1. マウスでパドルを左右に動かす
2. クリックでボールを発射
3. パドルでボールを跳ね返してブロックを破壊
4. すべてのブロックを破壊すればレベルクリア
5. ボールを落とすとライフ減少
6. ライフが0になるとゲームオーバー

### パドル反射
- パドルの中央に当たる：垂直に近い角度で反射
- パドルの端に当たる：水平に近い角度で反射
- 最大反射角度：左右60度

### ブロック耐久値
- 銀ブロック（⬜）：2回当てると破壊
- 金ブロック（🟫）：3回当てると破壊
- 残り耐久値は数字で表示

### パワーアップ
- ブロック破壊時に一定確率でドロップ
- パドルでキャッチして効果発動
- 複数のパワーアップを同時に発動可能

## 技術的特徴

### Canvas描画
```javascript
class BreakoutGame {
    draw() {
        // グリッド背景
        // グラデーションブロック
        // グラデーションパドル
        // 光沢のあるボール
        // パワーアップエフェクト
    }
}
```

### ゲームループ
```javascript
gameLoop() {
    if (this.gameOver || this.isPaused) return;

    this.update();  // ゲーム状態更新
    this.draw();    // 描画

    requestAnimationFrame(() => this.gameLoop());
}
```

### 衝突判定

#### パドルとの衝突
```javascript
// 当たった位置によって反射角度を変える
const hitPos = (ball.x - paddle.x) / paddle.width; // 0 to 1
const angle = (hitPos - 0.5) * 120 * Math.PI / 180; // -60° to +60°
```

#### ブロックとの衝突
```javascript
if (ball overlaps brick) {
    brick.hits--;
    if (brick.hits <= 0) {
        brick.visible = false;
        dropPowerup();
    }
    if (!ball.penetrate) {
        ball.dy = -ball.dy;  // 反射
    }
}
```

### パワーアップシステム
```javascript
activatePowerup(type) {
    const duration = 10000; // 10秒

    switch (type) {
        case 'expand':
            paddle.width = 180;
            setTimeout(() => paddle.width = 120, duration);
            break;
        case 'multi':
            // ボールを3つに増やす
            for (let i = 0; i < 2; i++) {
                balls.push(createBall());
            }
            break;
    }
}
```

## ビジュアル

### グラデーション効果
- ブロック：各色の明暗グラデーション
- パドル：青のグラデーション + ハイライト
- ボール：白の放射状グラデーション
- パワーアップ：単色 + 白枠

### UI要素
- スコア表示（大きく目立つ）
- ライフ表示（ハートアニメーション）
- レベル進捗バー
- 達成項目（スライドインアニメーション）

## パフォーマンス

- 軽量実装（外部ライブラリ不要）
- 効率的なCanvas描画
- スムーズな60FPS
- メモリ効率的な配列管理

## カスタマイズ

### パドルサイズ変更
```javascript
this.paddle = {
    width: 120,   // 通常時
    height: 15,
    // 拡大時は180
};
```

### ボール速度調整
```javascript
this.difficulties = {
    easy: { ballSpeed: 4, paddleSpeed: 8 },
    normal: { ballSpeed: 6, paddleSpeed: 10 },
    hard: { ballSpeed: 8, paddleSpeed: 12 }
};
```

### ブロックレイアウト変更
```javascript
createBricks() {
    const brickRowCount = 5 + Math.floor(this.level / 3);
    const brickColumnCount = 10;
    // レベルが上がるごとに行数増加
}
```

### パワーアップ効果時間
```javascript
const duration = 10000; // 10秒（ミリ秒）
```

## ブラウザ対応

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 攻略のコツ

### 初心者向け
1. **パドルの中央で受ける** - 安定した反射角度
2. **端のブロックから** - 確実に破壊
3. **焦らない** - ボールを落とさないことが最優先

### 中級者向け
1. **パドルの端を使う** - 意図的に角度をつける
2. **パワーアップ優先** - 銀・金ブロックを狙う
3. **マルチボール活用** - 一気に複数破壊

### 上級者向け
1. **反射角度を計算** - 効率的なルート作り
2. **パワーアップ組み合わせ** - 拡大+貫通など
3. **リスク管理** - ボール数とライフのバランス

## よくあるパターン

### 端のブロック処理
```
┌──────────────┐
│🟥🟥🟥🟥🟥🟥🟥│
│              │
│   パドル     │ ← 端に寄せて角度をつける
└──────────────┘
```

### マルチボール戦略
```
  🔴    🔴    🔴
   ↓     ↓     ↓
 🧱🧱🧱🧱🧱🧱🧱
      パドル
```
→ 複数のボールで一気に破壊

## レベル進行

- レベル1：5行のブロック
- レベル2：5行のブロック
- レベル3：5行のブロック
- レベル4：6行のブロック（3レベルごとに1行増加）
- ...

## 今後の拡張予定

- [ ] ボス戦（特殊な大型ブロック）
- [ ] 特殊ブロック（爆発、移動など）
- [ ] カスタムレベル作成
- [ ] リプレイ機能
- [ ] オンラインランキング
- [ ] 協力プレイモード
- [ ] サウンドトラック追加
- [ ] パーティクルエフェクト

## 技術スタック

- **HTML5**: Canvas API
- **CSS3**: Grid, Flexbox, Animations, Variables
- **JavaScript ES6+**: Classes, requestAnimationFrame, LocalStorage
- **Web Audio API**: サウンド効果

## ブロック崩しの歴史

- 1972年にAtariが「Pong」をリリース
- 1976年にAtariが「Breakout」をリリース
- スティーブ・ジョブズとスティーブ・ウォズニアックが開発に関与
- アーケードゲームの歴史に残る名作

## 世界記録

クラシックなアーケード版では：
- 最高得点：数百万点
- 最速クリア：数分台

## ライセンス

MIT License

---

**プレイのヒント**: パドルの位置でボールの角度をコントロールできます！
